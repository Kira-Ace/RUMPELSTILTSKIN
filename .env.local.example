# Frontend environment
# The API proxy reads the key from the root .env.local
VITE_API_PROXY=http://localhost:3001

# Backend environment (moved to root .env.local for shared access)
# VITE_GOOGLE_API_KEY=your-google-generative-ai-api-key-here
